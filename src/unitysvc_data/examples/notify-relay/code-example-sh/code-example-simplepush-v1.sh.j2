#!/bin/bash
# Code example for SimplePush notification relay services.
set -o pipefail

{% if local_testing %}
WEBHOOK_URL="{{ service_base_url }}${{'__webhook_path__'}}"

curl -sS --max-time 10 \
    -X POST \
    -H "Content-Type: application/json" \
    -d '{"msg":"connectivity check","event":"test"}' \
    -w "\nHTTP %{http_code}\n" \
    "$WEBHOOK_URL"

echo "sent"

{% else %}
GATEWAY_URL="{{ service_base_url }}"

curl -sS --max-time 10 \
    -X POST \
    -H "Content-Type: application/json" \
    -H "Authorization: Bearer ${UNITYSVC_API_KEY}" \
    -d '{"msg":"connectivity check","event":"test"}' \
    -w "\nHTTP %{http_code}\n" \
    "$GATEWAY_URL"

echo "sent"
{% endif %}
